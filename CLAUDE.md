# Documento legado (compatibilidade)

Este arquivo foi mantido apenas para compatibilidade histórica com fluxos Claude-first.

## Fonte canônica atual

- Regras principais: `AGENTS.md`
- Regras complementares: `.cursor/rules/*.mdc`
- Operação do framework: `.cursor/skills/`, `.cursor/agents/`, `.cursor/hooks.json`, `.cursor/mcp.json`

## Diretriz

Para novas mudanças, revise e atualize os artefatos em `AGENTS.md` e `.cursor/` em vez deste arquivo.
